The code in this project is very strange due to the jury rigging into handling databases

To install:
- run "npm install" in the "projekt04" folder
- run "node utils/setup_permissions.js". This is neccessary.
- to fill with dummy data run 'node utils/fillDB.js' in the terminal
